# =============================================================================
# YMaker / MakeVideo — Minimal Environment Configuration
# =============================================================================
# NOTE: LLM providers, API keys, and models are managed via the Settings UI
#       at http://localhost:3000/settings and stored in the database.
#       You do NOT need to set them here.
# =============================================================================

# ---------------------------------------------------------------------------
# Stock Video API Keys (optional — free sources work without keys)
# ---------------------------------------------------------------------------
PEXELS_API_KEY=
PIXABAY_API_KEY=

# ---------------------------------------------------------------------------
# NVIDIA NIM — Required ONLY if you use NVIDIA Magpie TTS voiceover
# ---------------------------------------------------------------------------
NVIDIA_API_KEY=

# ---------------------------------------------------------------------------
# Optional: Override defaults
# ---------------------------------------------------------------------------
# Frontend origin for CORS
FRONTEND_ORIGIN=http://localhost:3000

# FFMPEG binary path (default: ffmpeg)
FFMPEG_BINARY=ffmpeg
